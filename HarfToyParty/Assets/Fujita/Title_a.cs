using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Title_a : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    //taptostartをフワフワさせる

    //タイトルロゴを上にあげる
    //キャラをどうするか

    // Update is called once per frame
    void Update()
    {
        
    }
}
